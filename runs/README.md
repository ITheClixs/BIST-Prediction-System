# Run status

Research bundles are immutable evidence, not a leaderboard. A run can remain in history after a methodological defect is found; only the status below determines whether the README may cite it as accepted evidence.

| Run | Status | Reason |
|---|---|---|
| `20260728T223101Z-8b27df3-2a71b8` | accepted | Signal-time liquidity, fitted fold state, feature lineage, missingness, grouped attribution, action ledger, and portable scientific replay are all persisted. |
| `20260714T143541Z-112a94e-9d9b70` | superseded | Used full execution-session volume at the next open and an undocumented probability shrinkage in portfolio selection. Retained only so the correction is auditable. |

The accepted result is still negative after modeled transaction costs. See the generated experiment block in the root README for the exact metrics.
